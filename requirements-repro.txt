# Python environment used for final smoke tests of the submitted-repo snapshot.
# Install the package itself with: pip install -e .[dev]
sympy==1.14.0
numpy==2.3.5
scipy==1.17.0
pytest==9.0.2
matplotlib==3.10.8
